# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.production

# Python
backend/.venv
__pycache__
*.pyc

# Personal Claude Code session settings, not shared project config
.claude/settings.local.json

# Raw crawl data and its zip are multi-GB and excluded from deploys too (see .ebignore)
data_source.zip
crawled_notices/

# SQLite DB holds real (hashed) student credentials once the service is live;
# schema/seed data lives in init_db.py instead, so this shouldn't enter git history.
campus.db
backend/campus.db

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*

# 원격 저장소 최신화 전에 만든 로컬 백업 (2026-08-15). 공유 대상이 아님.
_local_backup_*/

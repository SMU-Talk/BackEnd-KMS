// 환경별로 이 값만 바꾸면 됩니다.
// 운영 서버로 배포할 때는 SMU_TALK_API_BASE를 실제 백엔드 주소로 바꾸고,
// manifest.json의 host_permissions에도 같은 주소를 추가하세요.
const SMU_TALK_API_BASE = "http://localhost:8000/api";
const SMUL_ORIGIN = "https://smul.smu.ac.kr";
